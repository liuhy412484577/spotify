{
  "name": "react-native-spotify",
  "version": "1.0.0",
  "description": "react-native module with spotify sdk for android and iOS",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf lib",
    "compile": "tsc -d"
  },
  "keywords": [
    "react-native",
    "spotify-remote",
    "spotify",
    "auth",
    "iOS",
    "android"
  ],
  "author": "liuhy412484577",
  "license": "ISC",
  "peerDependencies": {
    "react-native": ">=0.72.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
